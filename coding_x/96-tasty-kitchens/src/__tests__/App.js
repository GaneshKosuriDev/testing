import {render} from '@testing-library/react'

import App from '../App'

describe.skip('Tasty Kitchens App tests', () => {
  it('Sample test:::5:::', () => {
    // render(<App />)
    expect(true).toBe(true)
  })
})
